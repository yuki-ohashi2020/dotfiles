
CONFIG_ZSHRC_PATH="$CONFIG_PATH"/zsh/zshrc
# 必ずprompt優先
source "$CONFIG_ZSHRC_PATH"/prompt.zsh
source "$CONFIG_ZSHRC_PATH"/hook.zsh
source "$CONFIG_ZSHRC_PATH"/ls.zsh
source "$CONFIG_ZSHRC_PATH"/cd.zsh
source "$CONFIG_ZSHRC_PATH"/history.zsh


############################################################################
# Git
############################################################################
alias g='git'

############################################################################
# エディッター
############################################################################
alias vim="$EDITOR"
alias vi="$EDITOR"
# ! ToDo
    # alias vim='nvim'
    # alias vi='nvim'

############################################################################
# その他
############################################################################
alias cl='clear'
alias tm='tmux'

############################################################################
# chezmoi
############################################################################
alias ch='chezmoi'
alias cj="just -f $CHEZMOI_PATH/Justfile"


# ! ToDo オプションをつける
# s Commandでもいいかも
alias code-search='rg'
# ctrl+r
# コマンドで絞り込むか、コードで絞り込むか、ファイルで絞り込むか

# rg --line-number --column --no-heading --color=always "pattern" | fzf --ansi
# ! ToDo cmd+rでリロードにする
# alias reload="source $HOME/.zprofile"
# ! ToDo 起動時に読み込まれた設定を出力する方が良い
# alias showZenv="cat $HOME/.zshenv"

# ファイルを絞り込んでvimで開く
# vim finderの略
# alias vf="ag -l | peco | xargs -o vim"

# ! ToDo fdコマンド
# bat
# zsh-syntax-highlighting
# fzf

